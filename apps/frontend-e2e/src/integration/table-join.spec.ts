describe('table join', () => {
    it('should join a table', () => {
        // * Websocket is connected to table
        // * Should load join popup
        // * Fill in username form
        // * Click `join`
        // * User is placed in seat
    });
});
