describe('table create', () => {
    it('should create a table', () => {
        // * Should load table builder popup
        // * Fill in username form
        // * Click `create`
        // * Router updates to /:tableId
        // * Websocket is connected to table
    });
});
