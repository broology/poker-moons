export const roundMissingCards = 'The round does not have five cards on the table, so a winner cannot be determined.';
export const playerMissingCards = (playerId: string) => `${playerId} does not have two cards, so a winner cannot be determined.`