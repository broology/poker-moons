import { frontendSharedTimerMoon } from './frontend-shared-timer-moon';

describe('frontendSharedTimerMoon', () => {
    it('should work', () => {
        expect(frontendSharedTimerMoon()).toEqual('frontend-shared-timer-moon');
    });
});
