/**
 * @description The depth level of where the entities are relative to the table.
 *
 * - `0`: entity is at the front of the table
 * - `1`: entity is between the front and the middle of the table
 * - `2`: entity is between the middle and the back of the table
 * - `3`: entity is at the back of the table
 */
export type DepthLevel = 0 | 1 | 2 | 3;
